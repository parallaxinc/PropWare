set(PROPWARE_SOURCES
    ../c++allocate.h
    ../comparator
    ../filesystem/blockstorage
    ../filesystem/fat/fatfile
    ../filesystem/fat/fatfilereader
    ../filesystem/fat/fatfilewriter
    ../filesystem/fat/fatfs
    ../filesystem/file
    ../filesystem/filereader
    ../filesystem/filesystem
    ../filesystem/filewriter
    ../filesystem/sd
    ../hd44780
    ../i2c
    ../i2cbase
    ../l3g
    ../max6675
    ../mcp3000
    ../motor/stepper
    ../pcf8591
    ../pin
    ../port
    ../printcapable
    ../printer/printer
    ../printer/synchronousprinter
    ../PropWare
    ../queue
    ../runnable
    ../scancapable
    ../scanner
    ../seeedtft
    ../seeedtftfast
    ../seeedtftfast_as.S
    ../spi
    ../stringbuilder
    ../staticstringbuilder
    ../uart/abstractduplexuart
    ../uart/abstractsimplexuart
    ../uart/duplexuart
    ../uart/fullduplexuart
    ../uart/halfduplexuart
    ../uart/simplexuart
    ../uart/sharedsimplexuart
    ../uart/uart
    ../utility
    ../ws2812)

add_subdirectory(tests)

include_directories(${PROJECT_SOURCE_DIR})
add_subdirectory(cog)
add_subdirectory(cmm)
add_subdirectory(lmm)
add_subdirectory(xmmc)
add_subdirectory(xmm-single)
add_subdirectory(xmm-split)
